---
title: VPN
date: 2026-09-11
tags: [Network]
description: 공용 인터넷을 거치면서도 사설망처럼 안전하게 통신하게 해주는 VPN의 개념과 동작 원리
---

## VPN(Virtual Private Network)이란

특정 네트워크(또는 기기)가 공용 네트워크망을 지나면서도, 암호화된 통로를 통해 특정 사설망에 접근할 수 있도록 하는 기술

인터넷 자체는 누구나 지나다니는 공용 도로와 같아서, 그냥 통신하면 중간에 도청되거나 데이터가 변조될 위험이 있다. VPN은 두 지점 사이에 **암호화된 터널**을 만들어서, 그 안을 지나가는 데이터를 제3자가 볼 수 없게 만든다.

## 동작 원리

```
내 PC ──[암호화 터널]── 인터넷(공용망) ──[암호화 터널]── 회사 내부망
```

터널 안에서는 데이터가 암호화된 상태로 이동하고, 실제 통신하는 두 지점 입장에서는 마치 직통 사설 회선으로 연결된 것처럼 동작한다. 그래서 이름에 "Private"이 들어간다.

동작을 나눠보면 크게 두 가지 일이 일어난다.

- **캡슐화(Encapsulation)**: 원래 데이터를 통째로 암호화해서 새로운 패킷 안에 감싼다. 중간에서 패킷을 가로채도 내용물을 알 수 없다.
- **인증(Authentication)**: 터널을 만들기 전에 접속하는 사람/장치가 정당한 사용자인지 확인한다. 아무나 터널에 들어올 수 없게 막는 절차다.

## 주요 용도

- **재택근무/원격 접속**: 집에서 회사 VPN에 접속하면, 마치 사무실 내부망에 있는 것처럼 사내 시스템(내부 DB, 사내 페이지 등)에 접근할 수 있다.
- **지사 간 연결 (사이트 투 사이트 VPN)**: 물리적으로 떨어진 지사들을 인터넷을 통해 하나의 내부망처럼 연결한다.
- **공용 와이파이 보안**: 카페 같은 공용 와이파이를 쓸 때 트래픽 도청을 막기 위해 개인이 VPN을 사용한다.
- **지역 제한 우회**: 다른 국가의 IP로 우회 접속한다 (원래 보안 목적은 아니지만 흔한 사용 사례다).

## VPN의 두 가지 방식

| | 원격 접속 VPN | 사이트 투 사이트 VPN |
| --- | --- | --- |
| 연결 대상 | 개인 기기 ↔ 회사 네트워크 | 네트워크 ↔ 네트워크 |
| 대표 예시 | 재택근무자가 사내망 접속 | 본사와 지사 네트워크 연결 |
| 설정 위치 | 각 사용자 기기에 VPN 클라이언트 설치 | 양쪽 네트워크의 라우터/방화벽에 설정 |
